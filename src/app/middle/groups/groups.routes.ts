import {Route} from '@angular/router';
import {GroupsComponent} from './groups.component';

export const MODULE_ROUTES: Route[] = [
  {path: 'groups', component: GroupsComponent},
  // {path: 'groups/:id', component: UserDetailComponent}
];


